{
    "swagger": {
        "outputDirectory": "./swagger",
        "entryFile": "./src/routes.ts",
        "host": "localhost:3000",
        "version": "1.0.0",
        "name": "Blog",
        "description": "Simple blog REST API",
        "license": "",
        "basePath": "/",
        "securityDefinitions": {
            "basicAuth": {
                "type": "basic"
            }
        }
    }
}
